Module AbrirUmaInstancia

   Public Sub Show(ByVal FRM As Form)

	 'If Application.OpenForms.OfType(Of Form)().Count() > 0 Then
	 '    Application.OpenForms.OfType(Of Form)().First().Focus()
	 'Else
	 '    Dim frmNomeDoForm As Form = New Form()
	 '    frmNomeDoForm.Show()
	 'End If



   End Sub
End Module
